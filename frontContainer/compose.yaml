version: '3'
services:
  frontcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 32174:32174
    volumes:
      - ./app:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
